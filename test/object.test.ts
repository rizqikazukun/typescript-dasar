describe('Object', () => {
    it('should support in typescript', () => {
        const person: { name: string, age: number, description?: string } = {
            name: "Rizqi",
            age: 25
        }
    })
})